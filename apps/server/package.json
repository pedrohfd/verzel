{
	"name": "server",
	"type": "module",
	"main": "src/index.ts",
	"scripts": {
		"build": "tsdown",
		"check-types": "tsc -b",
		"compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
		"dev": "tsx watch src/index.ts",
		"start": "node dist/index.mjs"
	},
	"dependencies": {
		"@fastify/cors": "^11.3.0",
		"@verzel/auth": "workspace:*",
		"@verzel/db": "workspace:*",
		"@verzel/env": "workspace:*",
		"better-auth": "catalog:",
		"dotenv": "catalog:",
		"drizzle-orm": "^0.45.2",
		"fastify": "^5.10.0",
		"zod": "catalog:"
	},
	"devDependencies": {
		"@types/node": "catalog:",
		"@verzel/config": "workspace:*",
		"tsdown": "^0.22.14",
		"tsx": "catalog:",
		"typescript": "catalog:"
	}
}
