BETTER_AUTH_SECRET=
TICKET_SIGNING_SECRET=
BETTER_AUTH_URL=http://localhost:3000
CORS_ORIGIN=http://localhost:3001

# Local Postgres via `bun run docker:up` (docker-compose.yml)
DATABASE_URL=postgres://verzel:verzel@localhost:5433/verzel
DATABASE_URL_DIRECT=postgres://verzel:verzel@localhost:5433/verzel

# TMDB API Read Access Token (v4 Bearer token), from
# https://www.themoviedb.org/settings/api
TMDB_ACCESS_TOKEN=
